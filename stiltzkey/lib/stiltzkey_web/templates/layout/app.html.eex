<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Stiltzkey</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <%= if assigns[:current_user] do %>
            <li>
              <%= link @current_user.username, to: user_path(@conn, :show, @current_user), class: "btn btn-default btn-xs" %>
            </li>
            <li>
              <%= link "Sign out", to: session_path(@conn, :delete), method: :delete, class: "btn btn-default btn-xs" %>
            </li>
            <% else %>
            <li>
              <%= link "Sign in", to: session_path(@conn, :new), class: "btn btn-default btn-xs" %>
            </li>
            <li>
              <%= link "Sign up", to: user_path(@conn, :new), class: "btn btn-default btn-xs" %>
            </li>
            <% end %>
          </ul>
        </nav>

        <a href="<%= page_path(@conn, :index) %>"<span class="logo"></span></a>
      </header>

      <%= if get_flash(@conn, :info) do %>
      <div class="alert alert-info alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert">
          &times;
        </button>
        <%= get_flash(@conn, :info) %>
      </div>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
      <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert">
          &times;
        </button>
        <%= get_flash(@conn, :error) %>
      </div>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
